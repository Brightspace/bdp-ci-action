name: 'Java Build'
description: 'Use Maven to build Java project'
inputs:
  ARTIFACTORY_MAVEN:
    description: 'The Artifactory access token for Maven'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
